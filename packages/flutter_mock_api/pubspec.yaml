name: flutter_mock_api
description: A comprehensive API mocking framework for Flutter. Mock HTTP APIs during development with contract-first design, realistic data generation, and configurable network simulation.
version: 1.0.0
homepage: https://github.com/joonapay/flutter_mock_api

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
